<li class="collection-item #ffab40 orange accent-2 hoverable" >
    
    <p>{{burger_name}} </p>

    {{!-- <div class="divider"></div>
    
    <br> --}}

    <button class="change-devoured btn-small waves-light waves-effect hoverable" data-id="{{id}}" data-newdevour="{{devoured}}">
        {{#if devoured}}EAT IT!{{else}}Regurgitate{{/if}}
    </button>

    <button class="delete-burger btn-small waves-light waves-effect hoverable" data-id="{{this.id}}">DELETE!</button>

    

</li>